<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="friendsgram.a.skw.dao.InfoDao">

	<insert id="insertInfo" parameterType="friendsgram.member.dto.Member_InfoDto">
		insert into Member_InfoDto values(DEFAULT,#{name},#{birth},#{age},#{gender},#{phone},#{email},#{address},#{content},#{school_name},#{school_period},#{school_major},#{career_nme},#{career_period},#{career_role},#{certificate_name},#{certificate_date},DEFAULT)
	</insert>
	

</mapper>